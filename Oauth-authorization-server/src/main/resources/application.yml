server:
  port: 9000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3307/user_registration
    password: godis4all
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true